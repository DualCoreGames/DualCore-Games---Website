# Architecture

> Auto-generated by /map on 2026-04-14

## Overview

A high-performance, studio-grade static website built with a "Systems-First" philosophy. It showcases game development services and portfolio work with a focus on engineering precision.

```
┌─────────────────────────────────────────┐
│              [index.html]               │
│             (Entry Point)               │
├─────────────────────────────────────────┤
│         [main.js / style.css]           │
│         (Logic & Presentation)          │
├─────────────────────────────────────────┤
│       [Google Apps Script / GTM]        │
│          (Data & Analytics)             │
└─────────────────────────────────────────┘
```

## Components

### Core Layout
- **Purpose:** Consistent navigation and branding across all pages.
- **Location:** `header`, `footer` elements in HTML files.
- **Features:** Responsive navigation with dropdown menus, mobile toggle, and unified footer with legal links.

### Hero System
- **Purpose:** High-impact visual landing with background video and dynamic messaging.
- **Location:** `.hero` section in `index.html`.
- **Logic:** `assets/js/main.js` handles mouse-tracking parallax for the network animation and Dynamic Keyword Insertion (DKI) via `utm_term`.

### Component System
- **Services Grid:** Premium cards in `services/` and `index.html` using a grid layout.
- **Work Grid:** Portfolio items organized by category (Game Dev, Art, VR/XR).
- **Lead Capture Form:** Standardized AJAX-based form submission to a Google Apps Script backend.

## Data Flow

1. **User Arrival:** URL parameters (`utm_term`, `intent`) are parsed by `main.js`.
2. **UI Update:** Headline and form placeholders are updated based on intent.
3. **Form Submission:** User submits `#contactForm` -> AJAX POST to Google Apps Script -> Redirect to `thank-you.html`.
4. **Analytics:** GTM tracking triggered for specific conversion intents.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Google Tag Manager | Analytics | Event and conversion tracking. |
| Google Apps Script | Backend | Form data processing and notification. |
| Google Fonts | Assets | Typography (Inter). |

## Technical Debt

- [ ] **Binary matches in grep:** `grep` found matches in `hero.mp4` and some images; ensure these are actually intended (likely due to text embedded in metadata or similar).
- [ ] **TODOs:** Found "Next Wave TODO" in `PROJECT_RULES.md`.
- [ ] **Redundancy:** Some inline styles observed in `index.html` (e.g., margins, padding) that could be moved to `style.css`.

## Conventions

**Naming:** BEM-like naming for some classes (`btn-primary`, `nav-toggle`), but mostly semantic and descriptive.
**Structure:** Traditional static asset folder structure (`assets/css`, `assets/js`, `assets/images`, `assets/video`).
**Testing:** No automated testing suite identified (Manual verification required).
